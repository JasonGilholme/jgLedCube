language: cpp

sudo: required
dist: trusty

before_install:
 - sudo apt-get -qq update
 - sudo apt-get install -y libgtest-dev
 - "cd /usr/src/gtest && sudo cmake . && sudo cmake --build . && sudo mv libg* /usr/local/lib/ ; cd -"

compiler:
- gcc

os:
- linux

before_script: cmake -DTARGET_HARDWARE=PC -DCMAKE_CXX_COMPILER=c++
script: make && jgLedCube/pc/PC_tests
